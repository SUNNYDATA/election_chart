// buildCandidateSelect()
//
// function buildCandidateSelect(){
//   // var candidates = naicsCodes()
// var candidates = candidateCodes()
//   console.log(candidates)
//   candidates.forEach(function(industry){
//     // var optionTag = $("<option value='" +  industry.code + "'>" + industry.description + "</option>")
//     //var optionTag = $("<option value='" +  industry.candidate_id + "'>" + industry.candidate_name + "</option>")
//     //lines 12 and 13 may need it and listens to the candidate name
//      var optionTag = $("<option value='" + industry.candidate_id + "'>" + industry.party_full + "</option>")
//     $(".candidates").append(optionTag)
//   })
//
//   $('.candidates').on("change", function(){
//     buildGraph($(this).val())
//   })
 //}






     // var DmoneyRaised = _.pluck(groupedCandidates["Democratic Party"], 'total_receipts');
     // var DmoneySpent = _.pluck(groupedCandidates["Democratic Party"], 'total_disbursements');
     // var DscatterData = _.zip(DmoneyRaised, DmoneySpent);
     //
     // var RmoneyRaised = _.pluck(groupedCandidates["Republican Party"], 'total_receipts');
     // var RmoneySpent = _.pluck(groupedCandidates["Republican Party"], 'total_disbursements');
     // var RscatterData = _.zip(RmoneyRaised, RmoneySpent);



         //_.groupBy(['one', 'two', 'three'], 'length');
         //=> {3: ["one", "two"], 5: ["three"]}
         //var candidateName = _.pluck (results, 'candidate_name');

         
